// Opportunity Center metric card — reuses the shared PremiumMetricCard so the
// command-engine KPIs match the dashboard's premium treatment (animated counter,
// trend, hover lift).
export { default } from '../dashboard/PremiumMetricCard';
export type { PremiumMetricCardProps as OpportunityMetricCardProps } from '../dashboard/PremiumMetricCard';
