# M02 Authentication, Identity and Sessions — Feature Inventory

Pod: Identity Pod. Embedded consultant: IAM/zero-trust/MFA/session-security consultant. Independent reviewer: separate identity penetration-test consultant. Data: SENSITIVE. Dependencies: M01, M23; consumed by every protected module.

| ID | Feature and value | Roles / journeys | UI/API trace | Data/jobs/integrations | Controls, audit, tenant, PHI, flags/demo | Evidence / missing / production acceptance | Status |
|---|---|---|---|---|---|---|---|
| M02-F01 | Staff password login | All staff; valid, bad password, unknown/inactive/locked/suspended | `/login` → `POST /v1/auth/login` | `User`; no job/provider | Constant-work generic 401, tenant choice fail closed, audit with IP/UA; dev token non-prod only | Independent auth challenge accepted enumeration/rate controls; browser login passes | COMPLETE |
| M02-F02 | Access/refresh session rotation | Staff; initial, refresh, replay, expiry, cross-origin | session hook; `/csrf`, `/refresh`, `/me`, `/session-info` | `User`; refresh cookie/JWT | HttpOnly refresh, memory CSRF, single-flight, revoke/rotate; no token logging | Auth/session and single-flight suites pass; deployed cross-site cookie evidence external to engineering | COMPLETE |
| M02-F03 | Logout and revocation | Staff/admin; logout, repeated logout, revoked user | `POST /logout`; `/security/sessions*` | `User` token version/session fields | CSRF, atomic revoke/audit, 401 after revoke | Integration tests pass; admin/browser session-management feature remains M05 consumer evidence | COMPLETE |
| M02-F04 | Password reset | Staff; request known/unknown, expiry, replay, confirm | `/password-reset/request`, `/confirm` | `PasswordResetToken`, `User`; delivery seam | Non-enumerating request, hashed token, TTL/single-use, audit; no default credentials | Security tests cover replay; live email delivery belongs M11 and is external | IN DISCOVERY |
| M02-F05 | TOTP setup/challenge/disable | Staff; enroll, confirm, login challenge, bad/replayed code, disable | `/mfa/status`, `/setup`, `/verify`, `/disable` | encrypted fields on `User` | Secret encryption, MFA-success-only reset/audit, rate limit | Independent platform/staff auth evidence supports core; browser enrollment/recovery/accessibility not reconciled | IN DISCOVERY |
| M02-F06 | Distributed brute-force and rate-limit failure policy | Public auth; bursts, store unavailable, multi-instance | Fastify rate limit + auth route configs | Redis M21 integration | Production store required/fail closed; no local silent downgrade | Independent fail-closed store tests accepted; production Redis evidence external | COMPLETE |
| M02-F07 | RBAC permission resolution at authentication boundary | Staff roles; default, tenant override, denied action | auth claims + `requirePermission` | `User`, consumes M05 `RoleDefinition` | Tenant-bound role; server enforcement; denied response no PHI | RBAC negatives exist; exhaustive endpoint-permission map remains M05 | IN DISCOVERY |
| M02-F08 | Identity-plane separation | Staff/platform/patient; token used against wrong plane | staff JWT, platform JWT, portal JWT | `User`; consumes M03/M10 identities | Token type/audience plane separation and separate DB roles | Identity-plane isolation tests pass | COMPLETE |

