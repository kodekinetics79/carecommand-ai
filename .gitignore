# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
server/generated

.projects/cache
.projects/vault
.projects/state.test.json
.projects/state.local.test.json
.playwright
.env
.env.*
!.env.example
# NOTE: .env.production is intentionally NOT un-ignored — it must never be committed.
# Production secrets live only in the platform's encrypted env store (Vercel/Render).
api/_bundle.mjs
